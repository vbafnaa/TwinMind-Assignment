# Optional: Hugging Face Inference API token for the legacy Assistant
# (BERT fill-in suggestions in UserInterface.jsx). Never commit a real token.
# Copy to .env.local and restart npm start.
REACT_APP_HF_TOKEN=
